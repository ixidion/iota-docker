[Unit]
Description=IRI Docker Container with name {{ iri_hostname }}
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker start -a %p
ExecStop=-/usr/bin/docker stop %p
Restart=on-failure
RestartSec=10s

[Install]
WantedBy=multi-user.target
